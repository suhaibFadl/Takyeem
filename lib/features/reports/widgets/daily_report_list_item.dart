import 'package:flutter/material.dart';

class DailyReportListItem extends StatelessWidget {
  const DailyReportListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
